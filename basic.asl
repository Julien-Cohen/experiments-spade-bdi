!start.

+!start <-
    +car(red);
    +truck(blue).

+car(Color)
 <- .print("The car is ",Color).

-car(Color)
 <- .print("The car is not necessarily ",Color).

 +sun(Color)
 <- .print("The sun is ",Color).